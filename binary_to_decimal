    
#include<math.h>
#include<stdio.h>
int main(){
    int n;
    int rem;
    scanf("%d", &n);
    int sum =0;
    
    int base =1;
while(n!=0){
    rem = n%10;
    sum  += rem*base;
    base = base*2;
    n = n/10;
}
printf("%d", sum);
}
